# Server
PORT=3000
NODE_ENV=development

# Access Control (V1 API)
# Allowed origins for public mode (comma-separated)
# Public mode allows requests from these origins without API key
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# API keys for private mode (comma-separated)
# Private mode allows requests from anywhere with valid API key
API_KEYS=your-api-key-1,your-api-key-2
